# 读者卡（audience-card）

## 目标读者画像（选定）

- **是谁**：<待填>
- **已掌握**：<待填 —— 这类读者已经知道什么、不需要科普什么>
- **关心什么**：<待填>
- **痛点**：<待填 —— 真实困扰、未被满足的需求>
- **期待收获**：<待填 —— 知识/认知/工具/情绪>

## 候选画像区

> 主程序用 audience-confirm 技能，向用户呈现 2-3 个候选画像，让用户做选择题。

### 候选 A
<待填 画像描述 + 一句话差异>

### 候选 B
<待填 画像描述 + 一句话差异>

### 候选 C（可选）
<待填 画像描述 + 一句话差异>

## 确认状态

```
confirmed: false
```

> 用户选定后置为 `true`，主程序据此把 `audience_locked` 置 `true`（Gate A 通过）。
